---
---

test: add coverage reporting/thresholds and close branch-coverage gaps in `useFormCrashRecovery/rhf`, `internal/idb`, `useIndexedDB/changes`, and `useKeyboardScope/manager`

No published code changed — this only adds `@vitest/coverage-v8`, wires `npm run test:coverage` into CI, and adds/extends unit tests. Kept as an empty changeset (no version bump) for the release log.
